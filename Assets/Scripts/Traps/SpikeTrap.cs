using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpikeTrap : TrapProto
{
    // Start is called before the first frame update
    override protected void Affect(GameObject gameObject)
    {
        //gameObject.GetComponent<Player>().ApplayDamage() 
    }
}
